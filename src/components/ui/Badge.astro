---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<span
  class={`inline-block px-3 py-1 bg-gray-100 dark:bg-[#222949] rounded-full text-sm text-gray-700 dark:text-slate-300 ${className}`}
>
  <slot />
</span>
